name: drizzle_app
description: A weather forecast app being developed in Dart and Flutter

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+4
  geojson: ^0.9.1
  cupertino_icons: ^0.1.2

dev_dependencies:
  test: ^1.9.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
